# Vite React Template

A template for a Vite React app. It is preconfigured with
commitlint
eslint airbnb
prettier

It uses husky to enforce rules pre-commit.

## Quickstart

```
npm install
npm run dev
```
 
